mutation POSTING_RATTING(
  $account: String
  $acronym: String!
  $schema: String
  $document: DocumentInput
) {
  createDocument(
    account: $account
    acronym: $acronym
    schema: $schema
    document: $document
  ) {
    documentId
  }
}
