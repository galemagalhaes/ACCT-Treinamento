mutation POST_DATA(
  $acronym: String!
  $account: String!
  $schema: String
  $document: DocumentInput
) {
  createDocument(
    acronym: $acronym
    account: $account
    schema: $schema
    document: $document
  ) {
    documentId
  }
}
