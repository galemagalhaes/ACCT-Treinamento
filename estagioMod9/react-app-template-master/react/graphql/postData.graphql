mutation DocumentResponse(
  $account: String!,
  $acronym: String!,
  $document: DocumentInput
) {
  createDocument(
    account: $account,
    acronym: $acronym,
    document: $document
  ) {
    documentId
  }
}
